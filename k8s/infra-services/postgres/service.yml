apiVersion: v1
kind: Service
metadata:
  name: wa-postgres
  namespace: lissa-fit
  labels:
    app: wa-postgres
spec:
  type: ClusterIP
  selector:
    app: wa-postgres
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres