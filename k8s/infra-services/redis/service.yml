apiVersion: v1
kind: Service
metadata:
  name: wa-redis
  namespace: lissa-fit
  labels:
    app: wa-redis
spec:
  type: ClusterIP
  selector:
    app: wa-redis
  ports:
    - port: 6379
      targetPort: 6379
      name: redis